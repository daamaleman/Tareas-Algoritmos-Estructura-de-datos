
class Patient:
    def __init__(self, name, age, symptom, priority):
        self.name = name  # Nombre completo
        self.age = age  # Edad
        self.symptom = symptom  # Síntoma principal
        self.priority = priority  # Prioridad de atención (1 a 5)
